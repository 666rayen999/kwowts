const Error = () => (
  <section className="w-full h-full flex flex-col items-center justify-center">
    <h1 className="text-3xl font-bold text-red-light dark:text-red-dark">
      There is an ERROR happened!
    </h1>
  </section>
);

export default Error;
